{
  "name": "sourcecourt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A source-bound cross-examination lab for learning evidence-based reasoning.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "configure": "node scripts/configure.mjs",
    "smoke:live": "node scripts/live-smoke.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --check public/app.js && node --check public/metrics.mjs && node --check scripts/configure.mjs && node --check scripts/live-smoke.mjs && node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
